# Base Role

The Base role prepares every server before any other role is executed.

Responsibilities:

- Install base packages
- Configure hostname
- Configure time synchronization
- Configure firewalld
- Configure SELinux
- Disable swap (optional)
- Configure sysctl
- Configure SSH
- Cleanup
