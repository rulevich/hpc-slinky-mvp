---
- name: Disable firewalld on Kubernetes nodes for MVP
  hosts: kubernetes
  become: true

  tasks:
    - name: Stop firewalld
      ansible.builtin.service:
        name: firewalld
        state: stopped
        enabled: false
